//
//  Modifier.swift
//  Words
//
//  Created by Alumno on 06/03/25.
//

/*
import SwiftUI

// Estilo para botones redondeados
struct RoundButtonModifier: ViewModifier {
    var backgroundColor: Color
    var foregroundColor: Color

    func body(content: Content) -> some View {
        content
            .font(.headline)                       // Texto destacado
            .foregroundColor(foregroundColor)      // Color de texto
            .padding(.vertical, 10)                // Altura mínima
            .frame(maxWidth: .infinity)            // Ocupa todo el ancho posible
            .background(backgroundColor)           // Fondo del botón
            .clipShape(Capsule())                  // Forma de cápsula
    }
}

// Extensión fácil de aplicar en cualquier View
extension View {
    func roundButtonStyle(backgroundColor: Color, foregroundColor: Color) -> some View {
        self.modifier(RoundButtonModifier(backgroundColor: backgroundColor,
                                          foregroundColor: foregroundColor))
    }
}
*/
